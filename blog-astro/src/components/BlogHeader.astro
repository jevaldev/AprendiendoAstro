---

---

<section>
  <div class="wrapper">
    <h1>Blog personal</h1>
    <p>
      En este apartado estaré subiendo todo lo que ire aprendiendo sobre el
      desarrollo de aplicaciones web, asi como otros temas de interes de la
      carrera como lo son las <strong>bases de datos</strong>, <strong
        >arquitecturas de software</strong
      >, <strong>experiencia e interfaz de usuario</strong>
      <abbr title="User experience/User Interface">(UX/UI)</abbr> entre otros.
    </p>
  </div>
</section>

<style>
  section {
    position: relative;
    isolation: isolate;
    max-width: 1200px;
    margin: 4rem auto;
    padding: 3rem;
  }

  section::after {
    content: "";
    position: absolute;
    inset: 0;
    background: linear-gradient(
        rgba(255, 255, 255, 0.7),
        rgba(255, 255, 255, 0.7)
      ),
      var(--clr-tertiary);
    border-radius: 16px;
    z-index: -1;
    filter: blur(10px);
  }

  .wrapper {
    width: min(100% - 2rem, 80ch);
    margin: auto;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  h1 {
    font-size: 2.5rem;
    color: var(--clr-primary);
    font-weight: 700;
    margin: 0;
  }

  p {
    font-size: 1.15rem;
    line-height: 1.5;
    margin: 0;
  }

  strong {
    color: var(--clr-primary);
  }
</style>
